# Distribution output (build_and_distribute.bat)
dist/

# CMake / build trees
build/
cmake-build-*/
out/
_build/
install/

CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
compile_commands.json
CTestTestfile.cmake
Testing/

# Visual Studio / MSVC
.vs/
*.sln
*.vcxproj
*.vcxproj.filters
*.vcxproj.user
*.suo
*.user
*.userosscache
*.opensdf
*.sdf
*.pdb
*.ilk
*.idb
*.ipch

# Binaries and objects
*.exe
*.dll
*.so
*.dylib
*.a
*.lib
*.obj
*.o
*.exp

# IDE
.idea/
.vscode/
*.swp
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

build-msvc/
Desktop/Release/

# Local env / secrets (server config, API keys)
.env
.env.*
*.local

# Bundled FFmpeg binaries (LGPL — local only, not pushed to GitHub)
third_party/ffmpeg/

*.zip

# Runtime session logs (created automatically - see logs/README.md)
logs/*
!logs/README.md

# Persisted runtime state (registered connectors, saved on shutdown/reloaded
# on startup - see DroidHost state persistence in cli/droidcli.cpp)
droidcli_state.json

# Persistent agent-turn memory (SQLite, see cli/memory_store.cpp)
droidcli_memory.sqlite3

# Last agent-turn session the TUI was on (see cli/tui.cpp - resumed on next launch)
droidcli_last_session.txt